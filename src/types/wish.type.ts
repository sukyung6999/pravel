export interface WishDataProps {
  category: string;
  contentId: string;
}
export interface WishListItemProps {
  category: string;
  contentId: string;
  contentTypeId: string;
  thumbnail: string;
  title: string;
}
