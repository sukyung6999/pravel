import LoadingSpinner from '@/components/common/loading/LoadingSpinner';

const Loading = () => {
  return (
    <LoadingSpinner style="fixed left-[50%] top-[50%] translate-x-[-50%] translate-y-[-50%]" />
  );
};

export default Loading;
