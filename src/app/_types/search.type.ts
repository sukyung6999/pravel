export interface searchList {
  ko: string;
  en: string;
}

export interface introList {
  img: string;
  alt: string;
  id: string;
}

export interface SearchState {
  search: {
    tab: string
  }
}